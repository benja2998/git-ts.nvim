# git-ts.nvim

You must first install git-ts. See [https://github.com/benja2998/git-ts](https://github.com/benja2998/git-ts) for info.

## Installation

Install it using your favorite plugin manager.

## API

To open a window, use `require("git-ts").open()`.
